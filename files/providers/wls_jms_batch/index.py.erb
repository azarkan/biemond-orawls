# Index for wls_jms_batch - always return empty to force creation
# Batch resources don't have persistent state in WebLogic

f = open_file("name;jmsmodule;queues;topics;connection_factories;subdeployments;jms_servers;quotas;domain", tmp_script)

# Don't add any entries - return empty index to force on_create to run
# add_index_entry(f, [domain + '/' + 'batch', 'batch', '[]', '[]', '[]', '[]', '[]', '[]', domain])

f.close()
report_back_success()
