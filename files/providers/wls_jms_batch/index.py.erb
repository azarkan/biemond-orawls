# Index for wls_jms_batch - returns batch configuration status

f = open_file("name;jmsmodule;queues;topics;connection_factories;subdeployments;jms_servers;quotas;domain", tmp_script)

# For batch resources, we just return a simple status
# The actual indexing of individual resources is handled by their respective types
add_index_entry(f, [domain + '/' + 'batch', 'batch', '[]', '[]', '[]', '[]', '[]', '[]', domain])

f.close()
report_back_success()
